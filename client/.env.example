# API base URL (no trailing slash). Used when the frontend calls the backend.
# Local dev: http://localhost:5000/api
# Production (Render): https://YOUR-SERVICE.onrender.com/api
REACT_APP_API_URL=http://localhost:5000/api
